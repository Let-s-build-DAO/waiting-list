export enum PlanDurationEnum {
  monthly = 30,
  yearly = 365,
  daily = 1,
}

export enum PlanIntervalEnum {
  yearly = 'yearly',
  monthly = 'monthly',
  daily = 'daily',
}

export enum CurrencyEnum {
  USD = 'usd',
  NGN = 'ngn',
  KES = 'kes',
  CAD = 'cad',
}
